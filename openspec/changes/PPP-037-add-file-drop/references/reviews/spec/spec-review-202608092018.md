# Review: PPP-037-add-file-drop

- **Date**: 2026/08/09 20:18 JST
- **Reviewer**: openspec-reviewer (general agent)
- **Iteration**: 3 / 10
- **Result**: PASS

## Summary

iteration 2 で指摘した Minor 4 件の解消確認と、修正後の proposal 全体の再レビューを実施した。4 件すべてが解消されていることを確認した（要件 1 の矛盾文言の修正・混在ドロップの動画優先の tasks.md への明記・proposal.md What Changes への反映・複数動画時の「先頭のみ」選定規則とシナリオの追加）。`openspec validate --strict` はパスし、フォーマット・内容面（実コード `MainContent.svelte` の排他ロジック・`validateVideo`・`files[0]` 先頭採用との整合、既存 spec `video-posting`・`sns-posting` との整合）・ドメイン境界・タスク計画もすべて妥当。新たな指摘は 0 件で、approval を妨げる問題はない。

## Previous Issues Resolution

| # | 前回指摘（iteration 2） | 現在の状態 | 解消状況 |
|---|---|---|---|
| 1 | 要件 1 の「画像以外のファイル種別は、動画として扱ってはならない」が要件 2 と矛盾する文言 | `specs/file-drop/spec.md` 11 行目は「画像以外のファイル種別は、画像として扱ってはならない (SHALL NOT)」に修正され、要件 2 の動画添付・混在ドロップと矛盾しなくなった（分類規則としての記述であり、要件 3 の無視挙動と重複はあるが矛盾ではない） | 解消 |
| 2 | 混在ドロップの動画優先が tasks.md に未明記 | タスク 1.3 に「画像と動画が混在する場合は動画のみを添付し、画像を追加しない。複数の動画が含まれる場合は先頭のみを使用する」を追記 | 解消 |
| 3 | proposal.md の What Changes に混在ドロップの動画優先が未反映 | What Changes 6 項目目に「画像と動画が混在するドロップでは動画を優先して添付し、画像は無視する」を追記。spec と proposal の対応が揃った | 解消 |
| 4 | 複数動画ドロップ時の選定規則未定義 | 要件 2 に「複数の動画がドロップされた場合、システムは先頭の動画ファイルのみを添付しなければならない (SHALL)」を追記し、シナリオ「複数の動画をドロップする」を追加 | 解消 |

## Issues

指摘なし（Critical 0 件 / Major 0 件 / Minor 0 件）。

## Checklist

### A. フォーマット検証
- [x] proposal.md に Why / What Changes / Impact セクションが存在する
- [x] 各 Requirement に SHALL / SHALL NOT が含まれる
- [x] 各 Requirement に `#### Scenario:` 形式のシナリオが 1 つ以上ある
- [x] シナリオ見出しは日本語 + 括弧内英語で project.md の言語規約に準拠
- [x] GIVEN / WHEN / THEN（+ AND）形式で記述されている
- [x] `openspec validate PPP-037-add-file-drop --strict` がパスする（exit 0）

### B. 内容面レビュー
- [x] 前回 4 件の Minor 指摘（要件 1 矛盾文言・tasks.md 混在優先・What Changes 混在優先・複数動画選定規則）がすべて解消されている
- [x] 動画ドロップ時の解除（画像・リプライ元・引用元）と実コード `onSelectVideo`（MainContent.svelte:102-106）の挙動が一致している
- [x] 画像ドロップ時の動画解除と実コードの `$: if (images.length > 0 && videoFile != null)`（MainContent.svelte:111-113）が一致している
- [x] 「複数の動画は先頭のみ添付」は実コードの `files[0]` 採用（MainContent.svelte:86）と整合している
- [x] validateVideo のエラー（100MB・3 分・読み込み不可）と spec の「既存の動画バリデーションと同じエラー」記述が整合している
- [x] video-posting の許容上限チェック（100MB・180 秒・メタデータ取得失敗）と spec の記述が整合している
- [x] sns-posting の「動画と画像の併用禁止」（88-102 行目）「動画とリプライ・引用の併用禁止」（178 行目）と spec の排他記述が整合している
- [x] 混在ドロップで動画がバリデーションに失敗する場合も、混在ルール（画像を添付しない）と動画検証ルール（添付せずエラー通知）の合成で挙動が確定しており矛盾しない
- [x] エッジケースの網羅（複数動画・混在・上限超過・メタデータ読取失敗・ハイライト解除）に不足がない

### C. ドメイン境界変更の影響分析
- [x] 変更はフロントエンドのみ（MainContent.svelte・image-func.ts 再利用）で、バックエンド API・DB・スキーマへの影響がない
- [x] バックエンド投稿 API（`postToSns`）のインターフェース変更がなく、既存の添付状態（images / videoFile）の操作のみ
- [x] proposal.md の Impact 記載（Affected specs / Affected code / Breaking changes: なし）が内容と一致している

### D. タスク計画レビュー
- [x] 各 Requirement / Scenario に対応するタスクが存在する（画像 1.3/1.4/1.7、動画 1.3/1.5/1.6、無視 1.8、ハイライト 1.2、preventDefault 1.9）
- [x] タスク 1.3 に混在ドロップの動画優先と複数動画の先頭採用が明記されている（Issue 2 解消）
- [x] 検証タスク（ビルド・svelte-check・実機確認）が存在する
- [x] tasks.md の記述（1.4 の loadImageAsDataURL 再利用、1.5 の validateVideo、1.6 の解除、1.7 の動画解除）と実コード・spec の間に齟齬がない

## Conclusion

**PASS**。iteration 2 の Minor 4 件はすべて解消されており、`openspec validate --strict` もパスした。修正後の spec は実コード（`onSelectVideo` の画像・リプライ元・引用元の解除と `files[0]` 先頭採用、`$:` リアクティブによる画像追加時の動画解除、`validateVideo` のエラー文言）および既存 spec（`video-posting`・`sns-posting`）と整合している。新たな指摘は 0 件で、approval 可能な状態にある。
