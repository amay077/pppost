# Review: PPP-031-add-spa-version-update

- **Date**: 2026/08/04 12:18 JST
- **Reviewer**: openspec-reviewer
- **Model**: opencode-go/deepseek-v4-flash
- **Iteration**: 2 / 10
- **Result**: PASS

## Summary

前回指摘の 5 件（Minor）はすべて修正済みであることを確認した。spec への「built_at 取得不能時の判定禁止」追記、古い場合を含むシナリオ拡張、proposal への相対パス取得・Non-Goals 明記、tasks.md のシナリオ別検証分割はいずれも適切。`openspec validate --strict` もパスし、新規指摘はパス表記の軽微な揺れ 1 件のみ。

## Critical Issues

なし

## Major Issues

なし

## Minor Issues

### Issue 1: `replace.cjs` のパス表記が frontend プレフィックスなしで揺れている
- **Severity**: Minor
- **Location**: `proposal.md`
- **Line**: 34
- **Description**: Impact の Affected code で「変更なし: `frontend/public/version.json` / `version.mustache` / `script/replace.cjs`」とあり、同一リスト内の他項目（`frontend/src/lib/MainContent.ts` 等）が `frontend/` プレフィックス付きなのに対し、`script/replace.cjs` のみプレフィックスなし。実パスは `frontend/script/replace.cjs`（実在確認済み）であり、パス表記が不揃い。
- **Suggestion**: `script/replace.cjs` を `frontend/script/replace.cjs` に統一する。

## Previous Issues Resolution

前回（Iteration 1）の指摘事項と修正状況:

| # | Issue | Severity | Status |
|---|-------|----------|--------|
| 1 | `window.built_at` が空文字の場合の挙動が spec に未定義 | Minor | **修正済み** - spec.md:4 に「現在の SPA の `built_at` が取得できない（空文字等）場合、システムは更新が利用可能であると判定してはならない (SHALL)」が追記された |
| 2 | サーバ側 `built_at` が現在より古い場合のシナリオが未定義 | Minor | **修正済み** - spec.md:12-16 に「バージョンが最新または古い場合」シナリオが追加され、「同じか古い」場合にボタン非表示と明記された |
| 3 | `version.json` の fetch パス解決方式が spec に未記載 | Minor | **修正済み** - proposal.md:15 に「現在の index.html と同一ディレクトリの相対パスで取得する（`vite base: "./"` により、デプロイ先パス（`/pppost/` 等）に依存しない）」が明記された |
| 4 | 更新後の URL に `?v=<時刻>` クエリが残存する | Minor | **修正済み** - proposal.md:28 の Non-Goals に「更新後の URL に残る `?v=` クエリの除去（`history.replaceState` による整形）は行わない」が追加された |
| 5 | シナリオごとの検証タスクが手動確認に集約されている | Minor | **修正済み** - tasks.md の 2.3 が 2.3.1（新バージョン存在時）/ 2.3.2（最新時）/ 2.3.3（取得失敗時）に分割された |

## Checklist

### A. フォーマット検証
- [x] Requirement に SHALL/MUST が含まれている
- [x] 各 Requirement に Scenario がある
- [x] MODIFIED 要件に元の内容が完全に記載（該当する MODIFIED なし。ADDED のみ）
- [x] proposal.md に Why/What Changes/Impact がある
- [x] openspec validate --strict がパス

### B0. Plan ↔ Proposal/Spec 整合性（plan.md がある場合）
- N/A: `references/plan-*.md` は存在しない（plan.md なし）

### B. 内容面レビュー
- [x] 要件間に矛盾がない（「サーバ側が新しい場合のみ判定 SHALL」と「同じか古い場合ボタン非表示」は整合）
- [x] 曖昧な表現がない（「適切に」「必要に応じて」等の使用なし）
- [x] シナリオがテスト可能（built_at の比較条件・取得失敗条件が具体的）
- [x] エッジケース・エラーケースが網羅されている（最新/古い/取得失敗/サーバ側 new の 4 ケース）
- [x] 影響範囲が妥当（MainContent.ts / MainContent.svelte のみ。既存ビルド成果物を再利用）
- [x] ビジネスロジックが整合（同一生成源の ISO 8601 文字列比較は妥当）
- [x] プロジェクトドキュメントと整合（GLOSSARY / 技術スタック / change-id 命名規約準拠）
- [x] Spec ↔ Proposal 整合: proposal の意図が spec で網羅されている（検知・判定・ボタン表示・再読込・Non-Goals）
- [x] Spec ↔ Proposal 整合: 暗黙の前提が spec で明文化されている（window.built_at、取得不能時の判定禁止、相対パスは明記あり）
- [x] Spec ↔ Proposal 整合: 処理経路が完結している（version 表示開 → fetch → 比較 → ボタン表示 → 押下 → 再読込）

### C. ドメイン境界変更の影響分析
- [x] API フィールド追加の影響を確認（該当なし）
- [x] DB 列値追加の影響を確認（該当なし）
- [x] 定数追加の影響を確認（該当なし）
- [x] テーブルへの新規レコードの影響を確認（該当なし）

### D. タスク計画レビュー
- [x] 各 Requirement に対応するタスクがある（Req 1 → 1.1-1.3、Req 2 → 1.4）
- [x] 各 Scenario を検証するタスクがある（2.3.1-2.3.3 が 3 シナリオ＋押下検証に対応）
- [x] タスクに対象ファイルが明記されている
- [x] タスクの順序が依存関係を考慮している（実装 1.x → 検証 2.x の順序）
- [x] 検証/テストタスクが含まれている（`npm run check` / `npm run build-prod` は package.json に実在確認済み）

## Conclusion

この proposal は OpenSpec の品質基準を満たしており、実装に進めることを推奨します。前回指摘 5 件はすべて修正済みで、新規指摘はパス表記の軽微な揺れ（Minor）1 件のみであり、実装をブロックするものではありません。Minor Issue 1 は実装着手前に修正することを推奨します。
